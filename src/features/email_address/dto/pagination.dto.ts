// Re-export shared pagination DTOs for backward compatibility
export { PaginationDto, PaginatedResponse } from '@shared/dto/pagination.dto';

// This file now serves as a re-export to maintain existing imports
// while using the shared DTOs from the common module
